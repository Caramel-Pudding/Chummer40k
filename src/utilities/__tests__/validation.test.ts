import {
  setMaxValidation,
  setMinValidation,
  setMinMaxValidation,
  modificationMinValidation,
  modificationMaxValidation,
  modificationMinMaxValidation,
} from "../validation";

describe("Set Min Validation", () => {
  it("noop", () => {
    expect(true).toBe(true);
  });
});
